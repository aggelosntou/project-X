#include <stdio.h> // this tells the compiler to include the standard input/output library. printf lives there.

int main(void) { //this defines the main function. int means it returns an integer and void means it takes no arguments
    printf("Hello, matrix-lib-c!\n");
    return 0; //this tells the operating system that the program finished successfully
}